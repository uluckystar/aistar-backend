<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.localaihub.aistar.module.user.mapper.UserMapper">

    <!-- 自定义的查询示例 -->
    <select id="selectUserByUsername" resultType="com.localaihub.aistar.module.user.entity.User">
        SELECT * FROM users WHERE username = #{username}
    </select>

</mapper>